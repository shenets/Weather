<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="60px"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ComboBox Name="Cities" Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" Margin="10px, 0" />

        <Label Name="TemperatureTitle" Target="{Binding ElementName=Temperature}" Grid.Column="1" Grid.Row="1" FontWeight="Bold" />
        <TextBlock Name="Temperature" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Margin="10px, 0"/>

        <Label Name="DescriptionTitle" Target="{Binding ElementName=Description}" Grid.Column="2" Grid.Row="1" FontWeight="Bold" />
        <TextBlock Name="Description" Grid.Column="2" Grid.Row="1" VerticalAlignment="Center" Margin="10px, 0"/>

    </Grid>
</Window>
